<Window x:Class="K4AdotNet.Samples.Wpf.Viewer.MainWindow"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="K4AdotNet Viewer Sample"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize">
    <StackPanel Margin="10" Orientation="Vertical">
        <CheckBox Margin="3" Padding="3" x:Name="checkBoxPlaybackDisableColor" VerticalContentAlignment="Center">Don't show color stream on playback (if any)</CheckBox>
        <CheckBox Margin="3" Padding="3" x:Name="checkBoxPlaybackDisableDepth" VerticalContentAlignment="Center">Don't show depth stream on playback (if any)</CheckBox>
        <CheckBox Margin="3" Padding="3" x:Name="checkBoxPlaybackFast" VerticalContentAlignment="Center">Play as fast as possible (don't try to keep original FPS)</CheckBox>
        <Button Margin="3" Padding="3" Content="Open MKV file for playback" Click="OpenPlayback"/>
    </StackPanel>
</Window>
